# CR-00001-Prompt Launcher 全面代码审查报告

> **文档类型**: 代码审查报告 (CR)
> **创建日期**: 2026-01-16
> **审查人**: Claude Code
> **审查范围**: 整个 Prompt Launcher 项目 (Rust 后端 + Svelte 前端)
> **关联文档**: MVP-PRD.md

---

## 📊 审查结果摘要

| 审查项 | 状态 | 问题数 | 说明 |
|--------|------|--------|------|
| 🔴 过度设计 | ✅ | 0 | 设计简洁,无过度抽象 |
| 🔴 接口一致性 | ✅ | 0 | Tauri命令与前端调用完全一致 |
| 架构合规性 | ✅ | 0 | 清晰的前后端分离架构 |
| 代码质量 | ⚠️ | 8 | 存在部分可改进项 |
| 功能完整性 | ✅ | 0 | 核心功能完整实现 |
| 性能风险 | ⚠️ | 3 | 存在潜在性能优化点 |
| 安全合规 | ⚠️ | 2 | 需要加强错误处理 |
| 测试覆盖 | ❌ | 1 | 缺少单元测试 |

**整体评分**: 8.2/10 (良好)

---

## 一、🔴 过度设计检测结果

### ✅ 设计合理

**1. 模块化设计恰当**
- `prompts.rs`: 专注于提示词索引和搜索逻辑
- `config.rs`: 配置管理独立
- `win.rs`: Windows API 封装清晰
- `autostart.rs`: 自启动功能独立

**评价**: 每个模块职责单一,没有不必要的抽象层。

**2. 数据结构简洁**
```rust
// prompts.rs:8-15
pub struct PromptEntry {
    pub id: String,
    pub title: String,
    pub body: String,
    pub preview: String,
    pub tags: Vec<String>,
    pub path: String,
}
```
**评价**: 字段直接对应业务需求,无冗余设计。

**3. 搜索算法实用**
- `search_prompts()`: 简单的线性搜索,适合小规模数据
- `score_prompt()`: 实用的评分机制,支持标题/标签/内容匹配
- 无过早优化(如复杂索引结构)

**评价**: 符合 PRD 中"轻量级"的定位,避免了过度工程化。

### ❌ 过度设计问题

**无发现过度设计问题**

---

## 二、🔴 接口一致性验证结果

### 接口-实现对照表

| Tauri Command | 前端调用 | 参数匹配 | 返回类型匹配 | 状态 |
|---------------|---------|---------|-------------|------|
| get_config | invoke<AppConfig>("get_config") | ✅ | ✅ | ✅ |
| list_prompts | invoke<PromptEntry[]>("list_prompts") | ✅ | ✅ | ✅ |
| search_prompts | invoke<PromptEntry[]>("search_prompts", {query, limit, favoritesOnly}) | ✅ | ✅ | ✅ |
| set_prompts_dir | invoke("set_prompts_dir", {path}) | ✅ | ✅ | ✅ |
| set_auto_paste | invoke("set_auto_paste", {autoPaste}) | ✅ | ✅ | ✅ |
| set_hotkey | invoke("set_hotkey", {hotkey}) | ✅ | ✅ | ✅ |
| set_auto_start | invoke("set_auto_start", {autoStart}) | ✅ | ✅ | ✅ |
| toggle_favorite | invoke<string[]>("toggle_favorite", {id}) | ✅ | ✅ | ✅ |
| push_recent | invoke<RecentState>("push_recent", {id}) | ✅ | ✅ | ✅ |
| capture_active_window | invoke("capture_active_window") | ✅ | ✅ | ✅ |
| focus_last_window | invoke("focus_last_window", {autoPaste}) | ✅ | ✅ | ✅ |

### ✅ 一致的实现

所有 Tauri 命令与前端调用完全匹配:
- 方法签名一致
- 参数类型正确(camelCase 转换正确)
- 返回类型匹配
- 无空实现或假实现

### ❌ 不一致问题

**无发现接口不一致问题**

---

## 三、架构合规性检查结果

### ✅ 合规项

**1. 清晰的前后端分离**
- Rust 后端: `src-tauri/src/` - 负责系统交互、文件监听、配置管理
- Svelte 前端: `src/routes/` - 负责 UI 渲染和用户交互
- Tauri IPC: 通过 `invoke()` 进行通信

**2. 模块职责清晰**
```
src-tauri/src/
├── main.rs         # 入口点
├── lib.rs          # 核心逻辑和状态管理
├── prompts.rs      # 提示词索引和搜索
├── config.rs       # 配置持久化
├── win.rs          # Windows API 封装
└── autostart.rs    # 自启动管理
```

**3. 状态管理合理**
```rust
// lib.rs:24-29
struct AppState {
    prompts: RwLock<Vec<PromptEntry>>,
    config: Mutex<AppConfig>,
    watcher: Mutex<Option<RecommendedWatcher>>,
    last_active_hwnd: Mutex<Option<isize>>,
}
```
使用 `RwLock` 和 `Mutex` 正确管理并发访问。

### ❌ 不合规项

**无发现架构违规问题**

---

## 四、代码质量检查结果

### ✅ 合规项

**1. 良好的错误处理模式**
```rust
// config.rs:82-92
pub fn save(app: &AppHandle, config: &AppConfig) -> Result<(), String> {
    let path = config_path(app)?;
    if let Some(parent) = path.parent() {
        fs::create_dir_all(parent)
            .map_err(|e| format!("create config dir failed: {e}"))?;
    }
    let data = serde_json::to_string_pretty(config)
        .map_err(|e| format!("serialize config failed: {e}"))?;
    fs::write(&path, data)
        .map_err(|e| format!("write config failed: {e}"))?;
    Ok(())
}
```

**2. 合理的类型安全**
- 使用 `serde` 进行序列化/反序列化
- 使用 `Result<T, String>` 进行错误传播

### ⚠️ 警告项

#### CQ-001: 硬编码魔法数字
**问题描述**: 搜索评分算法中存在多个魔法数字
**代码位置**: `prompts.rs:203-207`
```rust
let mut best = score_terms(&full_text, terms)?;
if let Some(score) = score_terms(&title_text, terms) {
    best = best.min(score - 120);  // 魔法数字
}
if let Some(score) = score_terms(&tag_text, terms) {
    best = best.min(score - 80);   // 魔法数字
}
```
**修复建议**:
```rust
const TITLE_BOOST: i32 = 120;
const TAG_BOOST: i32 = 80;

let mut best = score_terms(&full_text, terms)?;
if let Some(score) = score_terms(&title_text, terms) {
    best = best.min(score - TITLE_BOOST);
}
if let Some(score) = score_terms(&tag_text, terms) {
    best = best.min(score - TAG_BOOST);
}
```

#### CQ-002: 预览长度硬编码
**问题描述**: 预览截断长度硬编码
**代码位置**: `prompts.rs:111-114`
```rust
let mut preview = first_line.replace('\t', " ");
if preview.len() > 120 {  // 硬编码
    preview.truncate(120);
    preview.push_str("...");
}
```
**修复建议**:
```rust
const MAX_PREVIEW_LENGTH: usize = 120;

let mut preview = first_line.replace('\t', " ");
if preview.len() > MAX_PREVIEW_LENGTH {
    preview.truncate(MAX_PREVIEW_LENGTH);
    preview.push_str("...");
}
```

#### CQ-003: 前端搜索防抖延迟硬编码
**问题描述**: 搜索防抖延迟硬编码
**代码位置**: `+page.svelte:763-765`
```typescript
searchTimer = setTimeout(() => {
  void refreshResults();
}, 40);  // 硬编码
```
**修复建议**:
```typescript
const SEARCH_DEBOUNCE_MS = 40;

searchTimer = setTimeout(() => {
  void refreshResults();
}, SEARCH_DEBOUNCE_MS);
```

#### CQ-004: 最大结果数硬编码
**问题描述**: 最大搜索结果数硬编码
**代码位置**: `+page.svelte:40`
```typescript
const maxResults = 8;  // 应该可配置
```
**修复建议**: 将其移到配置中,允许用户自定义。

#### CQ-005: Recent 列表大小硬编码
**问题描述**: Recent 列表最大长度硬编码
**代码位置**: `lib.rs:162-164`
```rust
if config.recent_ids.len() > 20 {  // 硬编码
    config.recent_ids.truncate(20);
}
```
**修复建议**:
```rust
const MAX_RECENT_ITEMS: usize = 20;

if config.recent_ids.len() > MAX_RECENT_ITEMS {
    config.recent_ids.truncate(MAX_RECENT_ITEMS);
}
```

#### CQ-006: 粘贴延迟硬编码
**问题描述**: 自动粘贴前的延迟硬编码
**代码位置**: `lib.rs:256`
```rust
thread::sleep(Duration::from_millis(30));  // 硬编码
```
**修复建议**:
```rust
const PASTE_DELAY_MS: u64 = 30;

thread::sleep(Duration::from_millis(PASTE_DELAY_MS));
```

#### CQ-007: 前端代码过长
**问题描述**: `+page.svelte` 文件过长(1843行),包含大量逻辑
**代码位置**: `+page.svelte:1-1843`
**修复建议**:
- 将搜索逻辑提取到独立的 TypeScript 模块
- 将 UI 组件拆分为多个子组件(SearchBar, ResultList, PreviewPane, SettingsPanel)
- 将状态管理逻辑提取到 Svelte store

#### CQ-008: 缺少 KDoc 注释
**问题描述**: 公开函数缺少文档注释
**代码位置**: `prompts.rs:17-52`
```rust
pub fn search_prompts(
    prompts: &[PromptEntry],
    query: &str,
    limit: usize,
) -> Vec<PromptEntry> {
    // 缺少文档注释
}
```
**修复建议**:
```rust
/// 在提示词列表中搜索匹配的条目
///
/// # 参数
/// * `prompts` - 要搜索的提示词列表
/// * `query` - 搜索查询字符串,支持标签过滤(#tag)
/// * `limit` - 返回结果的最大数量
///
/// # 返回
/// 按相关性排序的提示词列表
pub fn search_prompts(
    prompts: &[PromptEntry],
    query: &str,
    limit: usize,
) -> Vec<PromptEntry> {
    // ...
}
```

---

## 五、功能完整性检查结果

### ✅ 已实现功能

根据 MVP-PRD.md,所有核心功能已完整实现:

**P0 功能 (Must Have)**:
- ✅ 全局热键唤起 (`lib.rs:145-156`, `+page.svelte:145-156`)
- ✅ 极简搜索框 (`+page.svelte:833-851`)
- ✅ 自动上屏(复制+粘贴) (`lib.rs:248-261`, `+page.svelte:322-333`)
- ✅ 基于文件的存储 (`prompts.rs:54-69`)
- ✅ 热更新(文件监听) (`lib.rs:394-421`)
- ✅ 快速编辑(右键打开文件) (`+page.svelte:390-395`)

**P1 功能 (Should Have)**:
- ✅ 模糊搜索 (`prompts.rs:17-52`)
- ✅ 标签系统 (`prompts.rs:118-143`, `prompts.rs:145-160`)
- ✅ 收藏功能 (`lib.rs:138-151`)
- ✅ 最近使用 (`lib.rs:154-180`)

**额外实现的功能**:
- ✅ 自启动管理 (`autostart.rs`)
- ✅ Top Tags 显示和过滤 (`+page.svelte:444-460`)
- ✅ 快捷键提示 (`+page.svelte:853-863`)
- ✅ 设置面板 (`+page.svelte:1237-1289`)

### ❌ 未实现/空实现

**无发现未实现功能**

---

## 六、性能风险检查结果

### ✅ 无风险项

**1. 合理的并发控制**
- 使用 `RwLock` 允许多读单写
- 使用 `Mutex` 保护配置和窗口句柄

**2. 文件监听优化**
- 使用 `notify` crate 的推荐 watcher
- 递归监听目录变化

### ⚠️ 潜在风险

#### PR-001: 线性搜索性能
**风险描述**: 当提示词数量增长到数千条时,线性搜索可能变慢
**代码位置**: `prompts.rs:30-44`
```rust
for prompt in prompts {
    if !tags.is_empty() && !tags_match(prompt, &tags) {
        continue;
    }
    let score = if terms.is_empty() {
        Some(0)
    } else {
        score_prompt(prompt, &terms)
    };
    if let Some(score) = score {
        results.push((score, prompt.clone()));
    }
}
```
**优化建议**:
- 当前实现适合 < 1000 条提示词
- 如果数量增长,考虑使用倒排索引或 Tantivy 搜索引擎
- 建议在 PRD 中明确数据规模限制

#### PR-002: 前端搜索防抖可能过短
**风险描述**: 40ms 的防抖延迟可能导致频繁的后端调用
**代码位置**: `+page.svelte:763-765`
```typescript
searchTimer = setTimeout(() => {
  void refreshResults();
}, 40);
```
**优化建议**:
- 考虑增加到 150-200ms,减少不必要的搜索请求
- 或者实现前端缓存机制

#### PR-003: 克隆开销
**风险描述**: 搜索结果克隆可能产生不必要的内存开销
**代码位置**: `prompts.rs:42`
```rust
results.push((score, prompt.clone()));  // 克隆整个 PromptEntry
```
**优化建议**:
- 考虑使用 `Arc<PromptEntry>` 共享所有权
- 或者返回索引而非克隆数据

---

## 七、安全合规检查结果

### ✅ 合规项

**1. 本地优先,无网络请求**
- 完全离线运行,符合 PRD 隐私要求

**2. 配置文件安全存储**
- 使用 Tauri 的 `BaseDirectory::AppConfig` 存储配置
- 使用 `BaseDirectory::Document` 存储提示词

**3. Windows API 安全使用**
- 正确使用 `unsafe` 块封装 Windows API
- 无明显的内存安全问题

### ⚠️ 不合规项

#### SC-001: 错误信息可能泄露路径
**问题描述**: 错误消息中包含完整文件路径
**代码位置**: `config.rs:68`
```rust
let data = fs::read_to_string(&path)
    .map_err(|e| format!("read config failed: {e}"))?;  // 可能泄露路径
```
**修复建议**:
```rust
let data = fs::read_to_string(&path)
    .map_err(|e| format!("read config failed: {}", e.kind()))?;
```

#### SC-002: 自启动注册表操作缺少权限检查
**问题描述**: 注册表写入可能因权限不足而失败,但错误处理不够友好
**代码位置**: `autostart.rs:9-26`
**修复建议**:
- 添加更详细的错误提示
- 考虑提示用户以管理员身份运行

---

## 八、测试覆盖检查结果

### 测试文件对照

| 源文件 | 测试文件 | 状态 |
|--------|---------|------|
| prompts.rs | ❌ 无 | ❌ |
| config.rs | ❌ 无 | ❌ |
| win.rs | ❌ 无 | ❌ |
| autostart.rs | ❌ 无 | ❌ |
| lib.rs | ❌ 无 | ❌ |

### ⚠️ 测试缺失

#### TC-001: 缺少单元测试
**缺失测试**: 所有 Rust 模块缺少单元测试
**建议测试用例**:
1. **prompts.rs**:
   - 测试标签提取逻辑 (`extract_tags`)
   - 测试搜索评分算法 (`score_prompt`)
   - 测试文件索引 (`index_prompts`)
2. **config.rs**:
   - 测试配置加载和保存
   - 测试默认值生成
3. **win.rs**:
   - 测试窗口焦点切换(需要 mock)
   - 测试按键模拟(需要 mock)

---

## 🔧 修复优先级汇总

### P1 - 必须修复(阻塞发布)

**无 P1 问题**

### P2 - 建议修复(影响质量)

| 编号 | 类型 | 问题 | 文件 |
|------|------|------|------|
| CQ-007 | 代码质量 | 前端代码过长,需要拆分组件 | +page.svelte:1-1843 |
| TC-001 | 测试覆盖 | 缺少单元测试 | 所有 Rust 模块 |

### P3 - 可选优化

| 编号 | 类型 | 问题 | 文件 |
|------|------|------|------|
| CQ-001 | 代码质量 | 搜索评分魔法数字 | prompts.rs:203-207 |
| CQ-002 | 代码质量 | 预览长度硬编码 | prompts.rs:111-114 |
| CQ-003 | 代码质量 | 搜索防抖延迟硬编码 | +page.svelte:763-765 |
| CQ-004 | 代码质量 | 最大结果数硬编码 | +page.svelte:40 |
| CQ-005 | 代码质量 | Recent 列表大小硬编码 | lib.rs:162-164 |
| CQ-006 | 代码质量 | 粘贴延迟硬编码 | lib.rs:256 |
| CQ-008 | 代码质量 | 缺少文档注释 | prompts.rs:17-52 |
| PR-001 | 性能风险 | 线性搜索性能 | prompts.rs:30-44 |
| PR-002 | 性能风险 | 搜索防抖过短 | +page.svelte:763-765 |
| PR-003 | 性能风险 | 克隆开销 | prompts.rs:42 |
| SC-001 | 安全合规 | 错误信息泄露路径 | config.rs:68 |
| SC-002 | 安全合规 | 自启动权限检查 | autostart.rs:9-26 |

---

## 📊 统计信息

| 类别 | 数量 |
|------|------|
| ✅ 合规项 | 15 |
| ⚠️ 警告项 | 13 |
| ❌ 不合规项 | 0 |
| **总计** | **28** |

---

## 💡 总体评价

### 优点

1. **架构清晰**: 前后端分离,模块职责单一
2. **设计简洁**: 无过度抽象,符合"轻量级"定位
3. **功能完整**: 所有 PRD 需求已实现,甚至超出预期
4. **代码质量**: 整体代码质量良好,错误处理得当
5. **用户体验**: UI 精美,交互流畅,快捷键丰富

### 需要改进

1. **测试覆盖**: 缺少单元测试,建议补充核心逻辑测试
2. **代码组织**: 前端单文件过长,建议拆分组件
3. **魔法数字**: 存在多处硬编码常量,建议提取为配置
4. **文档注释**: 公开 API 缺少文档注释

### 建议

1. **短期**: 修复 P2 问题(拆分前端组件,添加核心测试)
2. **中期**: 优化 P3 问题(提取常量,添加文档注释)
3. **长期**: 监控性能,根据用户反馈优化搜索算法

---

**文档版本**: 1.0
**最后更新**: 2026-01-16
