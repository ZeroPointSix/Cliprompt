# 技术债清单：已弃用代码/依赖

1. 总体评价
未发现明确标记为 deprecated 的函数或 API 使用痕迹；当前依赖版本看起来较新，但缺少自动化依赖审计与升级策略。

2. 具体问题列表（按严重程度排序）
- ✅ 未发现代码层面的 deprecated 标记或弃用 API 调用（基于静态检索与人工抽查）。

3. 改进建议和示例代码
- 建议建立依赖审计流程（如 `npm audit`/`cargo audit`），并在发布前核查弃用/高危依赖。

示例（手动检查命令）：
```bash
npm audit
cargo audit
```
