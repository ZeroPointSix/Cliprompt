## 新增功能
- 新增“发送时追加剪贴板内容”开关，发送提示词时可自动拼接当前剪贴板文本。
- 设置页新增对应开关，默认关闭，按需启用。

## 修复
- 补齐剪贴板读取权限，避免读取失败导致拼接不生效。

## 重要变更
- 新增配置字段 `append_clipboard`（默认 false）。
- 版本号同步更新至 0.1.5（Tauri/Cargo/NPM）。

## 测试/验证
- `npm run check`
- `cargo test`
